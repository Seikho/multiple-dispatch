{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "command": "${workspaceRoot}/node_modules/.bin/tsc",
  "args": ["-p", "."],
  "problemMatcher": "$tsc",
  "tasks": [
    {
      "label": "${workspaceRoot}/node_modules/.bin/tsc",
      "type": "shell",
      "command": "${workspaceRoot}/node_modules/.bin/tsc",
      "args": ["-p", "."],
      "problemMatcher": "$tsc",
      "group": {
        "_id": "build",
        "isDefault": false
      }
    }
  ]
}
